{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug mini_gc",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": [],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,   // <- use external console
            "MIMode": "lldb",
            "logging": { "engineLogging": true }  // <- optional, for LLDB debug info
        }
    ]
}
